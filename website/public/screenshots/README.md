# Hero screenshots for the /plugin page

Drop two images here (any reasonable size, ~16:10 works best):

- `knmi-chart.png` — screenshot of the KNMI front chart
- `windy-overlay.png` — screenshot of Windy with the fronts overlay active

Until the files exist the page shows dashed placeholders with these paths.
